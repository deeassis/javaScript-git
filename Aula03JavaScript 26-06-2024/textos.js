const nomeAluno = "Denise";  // Declaração de uma constante nomeAluno do tipo string com o valor "Denise"
const idadeAluno = 18;  // Declaração de uma constante idadeAluno do tipo number com o valor 18
const casado = true;  // Declaração de uma constante casado do tipo boolean com o valor true
const time = ["Corinthians", "Santos"];  // Declaração de uma constante time do tipo array contendo os times "Corinthians" e "Santos"

console.log(typeof nomeAluno);  // Imprime o tipo da variável nomeAluno, que é string
console.log(typeof idadeAluno);  // Imprime o tipo da variável idadeAluno, que é number
console.log(typeof casado);  // Imprime o tipo da variável casado, que é boolean
console.log(typeof times);  // Imprime o tipo da variável times, que não foi declarada, então o resultado será "undefined"
